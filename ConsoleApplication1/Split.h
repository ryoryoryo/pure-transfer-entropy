#include <string>
#include <list>
#include <iostream>
#include <vector>
using namespace std;

list<string> splitList(string str, string delim);
vector<string> splitVector(const string &str, const string &delim);